const builder = require('electron-builder');

builder.build({
    config: {
        'appId': 'local.test.dockerode-electron',
        'win':{
            'target': {
                'target': 'zip',
                'arch': [
                    'x64',
                    'arm64',
                ]
            }
        }
    }
});
